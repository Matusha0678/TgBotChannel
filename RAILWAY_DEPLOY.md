# 🚀 Деплой бота на Railway

## 📋 Подготовка к деплою

### 1. Установите Railway CLI
```bash
npm install -g @railway/cli
```

### 2. Войдите в Railway
```bash
railway login
```

## 🚀 Процесс деплоя

### 1. Инициализация проекта
```bash
cd squadtgbot
railway init
```

### 2. Настройка переменных окружения
```bash
railway variables set BOT_TOKEN=7931654235:AAGN_PNWHFTOzkYSK83xbQi53qqh0C-ek4c
railway variables set GROUP_ID=-1003173587764
```

### 3. Деплой
```bash
railway up
```

### 4. Мониторинг
```bash
railway logs
```

## 📁 Структура файлов для деплоя

```
squadtgbot/
├── simple_bot.py          # Основной файл бота
├── requirements.txt       # Зависимости Python
├── railway.toml          # Конфигурация Railway
├── Procfile              # Команда запуска
├── runtime.txt           # Версия Python
├── .gitignore            # Игнорируемые файлы
├── .env                  # Переменные окружения (локальные)
└── README.md             # Документация
```

## 🔧 Конфигурация Railway

### railway.toml
- Указывает Python 3.11
- Команда запуска: `python simple_bot.py`
- Политика перезапуска при ошибках

### Procfile
- Определяет команду запуска для Heroku-совместимых платформ

### runtime.txt
- Фиксирует версию Python для стабильности

## 🎯 После деплоя

1. **Проверьте логи:**
   ```bash
   railway logs
   ```

2. **Проверьте переменные:**
   ```bash
   railway variables
   ```

3. **Тестирование:**
   - Отправьте `/start` в группе
   - Проверьте команды `/stats`, `/top`, `/holidays`

## 🚨 Возможные проблемы

### 1. Бот не отвечает
- Проверьте токен в переменных окружения
- Убедитесь что бот в группе и имеет права

### 2. Ошибки Python
- Проверьте версию Python (должна быть 3.11)
- Проверьте зависимости в requirements.txt

### 3. Проблемы с базой данных
- Railway создает временную БД при каждом деплое
- Для постоянного хранилища используйте Railway PostgreSQL

## 🔄 Автоматический деплой (опционально)

Для автоматического деплоя при изменениях в GitHub:

1. Создайте репозиторий на GitHub
2. Свяжите с Railway
3. Включите автоматический деплой

## 📊 Мониторинг

- **Логи:** `railway logs`
- **Метрики:** в дашборде Railway
- **Статус:** в дашборде проекта

---

🍆 *Готово к деплою на Railway!* 🍆
